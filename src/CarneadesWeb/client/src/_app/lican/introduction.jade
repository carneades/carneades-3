div(my-loading-spinner='viewLoading')
  h1 {{title}}
  p.introduction(translate='lican.intro.text')

  form(ng-submit='startAnalysis()')
    button.btn.btn-default(type='submit', translate='lican.intro.start')
