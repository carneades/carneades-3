.page-header(ng-show='hasReferences')
  h1 References
ul.references
  li(ng-repeat='r in references', id='{{r.key}}')
    [{{r.key}}] {{ r.creator }}. {{ r.date }}.
    a(href='{{ r.identifier }}')
      {{ r.title }}
